{
  "name": "MATE The AI Code Mentor",
  "version": "0.5.0",
  "description": "Get the best code review possible with a finely-tuned GPT",
  "scripts": {
    "dev": "webextension-toolbox dev",
    "start": "parcel watch src/manifest.json --host localhost --config @parcel/config-webextension",
    "build": "parcel build src/manifest.json --config @parcel/config-webextension"
  },
  "devDependencies": {
    "@babel/core": "^7.23.5",
    "@babel/preset-env": "^7.23.5",
    "@parcel/config-webextension": "^2.10.3",
    "babel-loader": "^9.1.3",
    "parcel": "^2.10.3",
    "parcel-transformer-manifest-version": "^1.1.0"
  }
}
